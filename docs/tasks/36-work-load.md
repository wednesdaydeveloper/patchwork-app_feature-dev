# #36 パッチワーク読み込み

- **ステータス**: `[ ]` 未着手
- **フェーズ**: 9. パッチワーク作成・編集画面
- **前提**: #35, #13

## 概要

`app/editor/[id].tsx` で既存 Work を読み込み、編集を再開できるようにする。

## 受け入れ条件 (AC)

- [ ] URL パラメータの id から Work を読み込み
- [ ] すべての PieceSetting が復元される
- [ ] 読み込み失敗時はエラーダイアログ → ホームへ戻る

## メモ

- 読み込み時に履歴 atom をクリア（CLAUDE.md「Undo/Redo」要件）。
