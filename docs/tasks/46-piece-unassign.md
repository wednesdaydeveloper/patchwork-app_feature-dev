# #46 ピース対応づけ解除

- **ステータス**: `[ ]` 未着手
- **フェーズ**: 13. UX 追加機能
- **前提**: #28, #29, #34

## 概要

選択中ピースの画像対応づけを UI から解除できるようにする。`removePieceSettingAtom` は既存。

## 受け入れ条件 (AC)

- [ ] 選択中ピースに割り当てがある場合、「対応づけを解除」ボタンを表示
- [ ] 押下で確認なしに即時解除（履歴に積むので Undo で戻せる）
- [ ] 解除後はピースが未割り当ての白塗り表示に戻る
- [ ] Undo / Redo で復元できる

## メモ

- ボタン位置は「調整」ボタンの近く。`FabricPicker` 内に配置するか、編集画面の操作行に置くかは設計時に決める。
- 解除前に `pushHistoryAtom` を呼ぶ（CLAUDE.md「Undo/Redo」要件）。
