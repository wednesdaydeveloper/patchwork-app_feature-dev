# #47 エディタ画面のワーク名表示・リネーム

- **ステータス**: `[x]` 完了
- **フェーズ**: 13. UX 追加機能
- **前提**: #35, #36

## 概要

編集中のパッチワーク名を画面上部に表示し、タップでリネームできるようにする。

## 受け入れ条件 (AC)

- [x] 編集中の Work 名（未保存時は「無題」）を画面上部に表示
- [x] タップで `PromptDialog` を開き名前を変更
- [x] 変更内容は `editingWorkNameAtom` に反映され、次の保存時に DB へ書き込まれる
- [x] リネーム自体は履歴に積まない（タイトル変更は本体の編集ではない）

## メモ

- 既存の `PromptDialog` と `editingWorkNameAtom` を再利用。
- ヘッダー（`<Stack.Screen>` の `title`）に直接表示するか、画面内バーに表示するかは UX で判断。
