# #24 布地登録（カメラ / カメラロール）

- **ステータス**: `[ ]` 未着手
- **フェーズ**: 7. 布地（FabricImage）管理機能
- **前提**: #08, #09, #23

## 概要

`expo-image-picker` でカメラ／カメラロールから画像を取得し、ドキュメントディレクトリに保存して DB に登録する。

## 受け入れ条件 (AC)

- [ ] カメラ／カメラロール選択 UI が動作
- [ ] 名前・カテゴリ入力ダイアログを表示
- [ ] 名前省略時はデフォルト名（例: `布地_yyyyMMddHHmmss`）
- [ ] パーミッション拒否時にエラーメッセージを表示
- [ ] 保存成功で一覧に即時反映

## メモ

- `app.json` のパーミッション文言は #45 で扱うが、ここで先行登録しても良い。
