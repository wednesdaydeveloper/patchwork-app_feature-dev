# #24 布地登録（カメラ / カメラロール）

- **ステータス**: `[x]` 完了
- **フェーズ**: 7. 布地（FabricImage）管理機能
- **前提**: #08, #09, #23

## 概要

`expo-image-picker` でカメラ／カメラロールから画像を取得し、ドキュメントディレクトリに保存して DB に登録する。

## 受け入れ条件 (AC)

- [x] カメラ／カメラロール選択 UI が動作
- [x] 名前・カテゴリ入力ダイアログを表示
- [x] 名前省略時はデフォルト名（例: `布地_yyyyMMddHHmmss`）
- [x] パーミッション拒否時にエラーメッセージを表示
- [x] 保存成功で一覧に即時反映

## メモ

- `app.json` のパーミッション文言は #45 で扱うが、ここで先行登録しても良い。
