# #01 Expo プロジェクト初期化

- **ステータス**: `[x]` 完了
- **フェーズ**: 1. プロジェクト基盤セットアップ
- **前提**: なし

## 概要

`create-expo-app` で TypeScript テンプレートを使ってプロジェクトを初期化する。

## 受け入れ条件 (AC)

- [x] `npx expo start` で開発サーバが起動する
- [x] `tsconfig.json` で `strict: true` が有効
- [ ] iOS / Android シミュレータで起動確認できる（ユーザー側で実施）

## メモ

- コマンド: `npx create-expo-app@latest --template blank-typescript`
