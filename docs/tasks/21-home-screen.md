# #21 ホーム画面 UI

- **ステータス**: `[ ]` 未着手
- **フェーズ**: 6. ホーム画面
- **前提**: #15, #19

## 概要

`app/index.tsx` に「新規作成」「布地管理」「設定」「保存済みパッチワーク一覧」を配置する。

## 受け入れ条件 (AC)

- [ ] 保存済みパッチワークが `FlatList` で一覧表示される
- [ ] 各導線ボタンから該当画面へ遷移
- [ ] 0 件時は空状態（メッセージ + 新規作成 CTA）を表示

## メモ

- 一覧アイテムは別コンポーネントへ切り出す（#22 で扱う）。
