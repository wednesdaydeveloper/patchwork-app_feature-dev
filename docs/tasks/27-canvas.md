# #27 キャンバス基盤コンポーネント

- **ステータス**: `[ ]` 未着手
- **フェーズ**: 9. パッチワーク作成・編集画面
- **前提**: #07, #10

## 概要

`components/canvas/PatternCanvas.tsx` を実装し、`react-native-svg` でピースを描画する。

## 受け入れ条件 (AC)

- [ ] 任意のパターンを正しい比率で描画
- [ ] 正規化座標 → 端末ピクセル座標の変換ユーティリティを実装
- [ ] `React.memo` で再描画最小化

## メモ

- ピース描画は子コンポーネント `Piece.tsx` に切り出す。
