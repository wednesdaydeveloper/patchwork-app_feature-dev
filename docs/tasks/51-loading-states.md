# #51 ローディング表示

- **ステータス**: `[ ]` 未着手
- **フェーズ**: 13. UX 追加機能
- **前提**: #21, #23, #36, #37

## 概要

DB / ファイルシステム読み込み中の空状態のチラつきを抑え、ローディング表示を出す。

## 受け入れ条件 (AC)

- [ ] ホーム画面：`worksLoadedAtom === false` の間はスピナー
- [ ] 布地管理画面：`fabricsLoadedAtom === false` の間はスピナー
- [ ] 編集画面：既存 Work 読み込み中はスピナー
- [ ] エクスポート画面：Work 読み込み中はスピナー
- [ ] 既存の「0 件」空状態は読み込み完了後にだけ表示

## メモ

- 共通 `Spinner` / `LoadingView` コンポーネントを `components/ui/` に追加するのが綺麗。
- `ActivityIndicator` を使えば実装は短い。
