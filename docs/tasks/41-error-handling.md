# #41 エラーハンドリング全般整備

- **ステータス**: `[x]` 完了
- **フェーズ**: 12. 横断的な品質保証
- **前提**: #20, #35, #38, #39

## 概要

CLAUDE.md「エラーハンドリング方針」表に従い、各箇所のエラー処理を整備する。`utils/logger.ts` を実装。

## 受け入れ条件 (AC)

- [x] パッチワーク保存／読み込み／画像エクスポート／印刷／パターン JSON パースの各エラーが定義通りトースト／ダイアログで表示
- [x] すべてのエラーが内部ログへ記録
- [x] `logger` は将来のクラッシュレポート連携を見据えた抽象化

## メモ

- `logger.error(category, message, meta)` のような API 設計。
