# #39 印刷用 PDF 出力

- **ステータス**: `[x]` 完了
- **フェーズ**: 10. エクスポート機能
- **前提**: #37

## 概要

`expo-print` で PDF を生成し、用紙サイズ選択と実寸スケール情報を含める。

## 受け入れ条件 (AC)

- [x] 用紙サイズ選択 UI（A4 など）
- [x] 実寸スケール情報を含む
- [x] 生成失敗時はエラーダイアログで原因を提示

## メモ

- 内部的に HTML を生成して `Print.printToFileAsync` で PDF 化する方式が一般的。
